## 서버 개요
클라이언트로부터 자연어 쿼리를 입력받아 유형을 파악한 후 해당 유형에 해당하는 인자를 뽑아냄.
그 다음 DB 조회/삽입 등의 작업을 거친 후에 사용자에게 적절한 자연어로 결과를 반환

## 구성 폴더
# database
db 스키마가 정의되어있고 서버와 db를 연결하는 파일
# sevice
쿼리 처리 및 db 작업 파일로 구성됨

## 필요 라이브러리
1. Uvicorn
2. FastAPI 

## 필요 라이브러리 설치방법
pip install -r requirements.txt